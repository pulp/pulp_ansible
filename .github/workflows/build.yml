# WARNING: DO NOT EDIT!
#
# This file was generated by plugin_template, and is managed by it. Please use
# './plugin-template --github pulp_ansible' to update this file.
#
# For more info visit https://github.com/pulp/plugin_template

---
name: "Build"
on:
  workflow_call:

defaults:
  run:
    working-directory: "pulp_ansible"

jobs:
  build:
    runs-on: "ubuntu-latest"

    steps:
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 1
          path: "pulp_ansible"
      - uses: "actions/setup-python@v5"
        with:
          python-version: "3.11"
      - name: "Install python dependencies"
        run: |
          echo ::group::PYDEPS
          pip install packaging twine wheel
          echo ::endgroup::
      - name: "Build package"
        run: |
          python3 setup.py sdist bdist_wheel --python-tag py3
          twine check dist/*
      - name: "Install built packages"
        run: |
          pip install dist/pulp_ansible-*-py3-none-any.whl
      - name: "Generate api specs"
        run: |
          pulpcore-manager openapi --file "api.json"
          pulpcore-manager openapi --bindings --component "ansible" --file "ansible-api.json"
      - name: "Upload Package whl"
        uses: "actions/upload-artifact@v4"
        with:
          name: "plugin_package"
          path: "pulp_ansible/dist/"
          if-no-files-found: "error"
          retention-days: 5
          overwrite: true
      - name: "Upload API specs"
        uses: "actions/upload-artifact@v4"
        with:
          name: "api_spec"
          path: |
            pulp_ansible/api.json
            pulp_ansible/ansible-api.json
          if-no-files-found: "error"
          retention-days: 5
          overwrite: true
