# WARNING: DO NOT EDIT!
#
# This file was generated by plugin_template, and is managed by it. Please use
# './plugin-template --github pulp_ansible' to update this file.
#
# For more info visit https://github.com/pulp/plugin_template


---
name: "Ansible CI Update"
on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    # runs at 14:30 UTC every Sunday
    - cron: '30 14 * * 0'
  workflow_dispatch:

jobs:
  update:
    runs-on: "ubuntu-latest"

    strategy:
      fail-fast: false

    steps:
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          repository: "pulp/plugin_template"
          path: "plugin_template"

      - uses: "actions/setup-python@v5"
        with:
          python-version: "3.11"

      - name: "Install python dependencies"
        run: |
          echo ::group::PYDEPS
          pip install gitpython packaging -r plugin_template/requirements.txt
          echo ::endgroup::

      - name: "Configure Git with pulpbot name and email"
        run: |
          git config --global user.name 'pulpbot'
          git config --global user.email 'pulp-infra@redhat.com'
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_ansible"
          ref: "main"

      - name: "Run update"
        working-directory: "pulp_ansible"
        run: |
          ../plugin_template/scripts/update_ci.sh

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_main"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_ansible"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch main"
          branch: "update-ci/main"
          base: "main"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_ansible"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_main.outputs.pull-request-number }}"
        if: "steps.create_pr_main.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_ansible"
          ref: "0.18"

      - name: "Run update"
        working-directory: "pulp_ansible"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_0_18"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_ansible"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 0.18"
          branch: "update-ci/0.18"
          base: "0.18"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_ansible"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_0_18.outputs.pull-request-number }}"
        if: "steps.create_pr_0_18.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_ansible"
          ref: "0.20"

      - name: "Run update"
        working-directory: "pulp_ansible"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_0_20"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_ansible"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 0.20"
          branch: "update-ci/0.20"
          base: "0.20"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_ansible"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_0_20.outputs.pull-request-number }}"
        if: "steps.create_pr_0_20.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_ansible"
          ref: "0.21"

      - name: "Run update"
        working-directory: "pulp_ansible"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_0_21"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_ansible"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 0.21"
          branch: "update-ci/0.21"
          base: "0.21"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_ansible"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_0_21.outputs.pull-request-number }}"
        if: "steps.create_pr_0_21.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_ansible"
          ref: "0.22"

      - name: "Run update"
        working-directory: "pulp_ansible"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_0_22"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_ansible"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 0.22"
          branch: "update-ci/0.22"
          base: "0.22"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_ansible"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_0_22.outputs.pull-request-number }}"
        if: "steps.create_pr_0_22.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_ansible"
          ref: "0.24"

      - name: "Run update"
        working-directory: "pulp_ansible"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_0_24"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_ansible"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 0.24"
          branch: "update-ci/0.24"
          base: "0.24"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_ansible"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_0_24.outputs.pull-request-number }}"
        if: "steps.create_pr_0_24.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_ansible"
          ref: "0.29"

      - name: "Run update"
        working-directory: "pulp_ansible"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_0_29"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_ansible"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 0.29"
          branch: "update-ci/0.29"
          base: "0.29"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_ansible"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_0_29.outputs.pull-request-number }}"
        if: "steps.create_pr_0_29.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
...
